# ReadMe

## Requirements
This config uses `vim.pack`; as such, you need at least version 0.12.

To be able to fully use all features of this config, install the following dependencies:

`sudo apt install gcc g++ ripgrep fd-find`
